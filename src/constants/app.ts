import { env } from "@/src/config/env";

export const APP_NOMBRE = env.EXPO_PUBLIC_APP_NAME;
export const APP_SCHEME = env.EXPO_PUBLIC_APP_SCHEME;
export const APP_BASE_URL = env.EXPO_PUBLIC_APP_BASE_URL;
